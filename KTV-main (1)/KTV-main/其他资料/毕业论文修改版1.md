# 重庆对外经贸学院

# 2026届本科毕业设计

**声域友线上社交KTV平台的设计与实现**

| 项目 | 内容 |
|------|------|
| 学院 | 数学与计算机科学学院 |
| 专业 | 计算机科学与技术 |
| 学号 | _______________ |
| 学生姓名 | _______________ |
| 指导教师 | _______________ |
| 完成日期 | 2026年5月 |

---

## 摘  要

随着互联网技术的快速发展和移动终端的普及，线上娱乐方式日益丰富。传统的线下KTV模式受限于场地和时间，难以满足用户随时随地进行社交娱乐的需求。本文设计并实现了一套名为"声域友"的线上社交KTV平台，旨在为用户提供通过浏览器和微信小程序进行在线K歌、实时聊天的社交娱乐体验。

系统采用前后端分离的B/S架构，后端基于ASP.NET Core 8.0框架，使用Dapper作为数据访问层，SQL Server作为数据库，通过RESTful API和SignalR实现实时通信；前端管理后台和用户端采用Vue 3 + TypeScript + Tailwind CSS技术栈，移动端采用微信小程序原生开发。系统实现了用户注册登录、虚拟房间管理、歌曲库管理、在线点歌排队、实时聊天、播放状态同步等核心功能。

本设计首先对系统进行了需求分析和概要设计，然后详细阐述了数据库设计、接口设计和各模块的详细设计与实现过程。系统测试结果表明，各功能模块运行稳定，能够满足线上社交KTV的基本业务需求，具有一定的实用价值。

**关键词**：线上KTV；社交娱乐；Vue 3；ASP.NET Core；微信小程序

---

## Abstract

With the rapid development of Internet technology and the popularization of mobile terminals, online entertainment methods have become increasingly diverse. The traditional offline KTV model is limited by venue and time, making it difficult to meet users' needs for social entertainment anytime and anywhere. This paper designs and implements an online social KTV platform named "Hydro-Sonic Canvas", aiming to provide users with social entertainment experiences of online karaoke singing and real-time chatting through browsers and WeChat Mini Programs.

The system adopts a front-end and back-end separated B/S architecture. The back-end is based on the ASP.NET Core 8.0 framework, using Dapper as the data access layer and SQL Server as the database, achieving real-time communication through RESTful API and SignalR. The front-end management console and user terminal use the Vue 3 + TypeScript + Tailwind CSS technology stack, while the mobile terminal adopts native WeChat Mini Program development. The system implements core functions including user registration and login, virtual room management, song library management, online song ordering and queuing, real-time chat, and playback state synchronization.

**Key words**: Online KTV; Social Entertainment; Vue 3; ASP.NET Core; WeChat Mini Program

---

## 目  录

1 绪论
1.1 研究背景
1.2 研究目的
1.3 国内外研究现状
1.4 研究内容
1.5 论文组织结构
2 相关技术介绍
2.1 前端技术
2.2 后端技术
2.3 数据库技术
2.4 实时通信技术
3 系统分析
3.1 可行性分析
3.2 需求分析
3.3 用例分析
3.4 系统分析模型
4 系统设计
4.1 系统架构设计
4.2 软件结构设计
4.3 数据库设计
4.4 接口设计
5 系统实现
5.1 开发环境
5.2 后端核心模块实现
5.3 管理后台核心模块实现
5.4 用户端核心模块实现
5.5 微信小程序核心模块实现
6 系统测试
6.1 测试策略
6.2 测试用例设计
6.3 测试结论
7 总结与展望
7.1 总结
7.2 展望
参考资料
致谢

---

## 1 绪论

### 1.1 研究背景

近年来，随着移动互联网技术的飞速发展和智能终端设备的广泛普及，人们的娱乐方式发生了深刻变革。传统的线下KTV作为重要的社交娱乐场所，长期以来深受广大用户的喜爱。然而，线下KTV模式存在诸多限制：首先，用户需要前往固定场所才能享受KTV服务，受到地理位置的约束；其次，KTV包厢通常需要提前预约，且营业时间有限，无法满足用户随时随地的娱乐需求；此外，线下KTV的消费成本相对较高，对于学生群体和年轻用户来说存在一定经济压力。

与此同时，社交网络和在线娱乐产业蓬勃发展，用户对于线上社交娱乐的需求日益增长。特别是在后疫情时代，远程社交和在线娱乐成为人们重要的生活方式。现有的在线音乐平台如网易云音乐、QQ音乐等虽然提供了丰富的音乐资源，但缺乏实时互动和社交功能，无法满足用户与朋友一起在线K歌的需求。因此，开发一套集在线K歌、实时社交、虚拟房间于一体的线上社交KTV平台，具有重要的现实意义和应用价值。

### 1.2 研究目的

本设计旨在开发一套名为"声域友"的线上社交KTV平台，主要研究目的包括：

（1）设计并实现一个功能完善的线上社交KTV系统，涵盖用户管理、歌曲管理、房间管理、在线点歌、实时聊天等核心功能，为用户提供便捷的在线K歌体验。

（2）采用前后端分离的现代化开发架构，前端使用Vue 3 + TypeScript技术栈，后端使用ASP.NET Core 8.0框架，实现系统的高内聚、低耦合，提高代码的可维护性和可扩展性。

（3）实现微信小程序端，使用户可以通过移动设备随时随地访问平台，扩大系统的使用场景和用户覆盖面。

（4）通过实时通信技术实现播放状态同步和即时聊天功能，提升用户的社交互动体验。

### 1.3 国内外研究现状

在在线KTV领域，国内外已有一些成熟的产品和研究成果。在国外，Smule、Karaoke等应用较早进入在线K歌市场，提供了丰富的歌曲库和社交功能。Smule应用支持用户录制和分享演唱视频，具有较强的社交属性，但主要面向海外市场，国内用户使用较少。

在国内，在线K歌市场发展迅速，唱吧、全民K歌等产品占据了较大的市场份额。唱吧作为国内较早的在线K歌平台，提供了实时对唱、合唱、PK等功能，用户规模庞大。全民K歌依托腾讯的社交生态，实现了与微信、QQ的深度整合，用户可以方便地分享演唱作品到社交平台。这些产品主要面向移动端用户，以手机APP为主要载体。

在技术实现方面，现有的在线K歌系统大多采用微服务架构，使用WebSocket实现实时通信，采用音频处理技术实现音效调节和伴奏分离。然而，这些系统普遍存在开发成本高、技术复杂度大的特点，对于中小型团队和学术研究项目来说，实现难度较大。

本设计在充分调研现有产品的基础上，结合实际需求和技术条件，采用轻量级的技术方案实现线上社交KTV平台。系统不追求复杂的功能和华丽的特效，而是聚焦于核心的K歌社交功能，注重系统的实用性和可实现性，为类似项目提供一个可行的技术参考方案。

### 1.4 研究内容

本设计的主要研究内容包括：

（1）系统需求分析：对线上社交KTV平台进行详细的需求调研和分析，明确系统的功能需求和非功能需求。

（2）系统架构设计：设计合理的系统整体架构，包括前后端分离架构、后端分层架构、数据库架构等。

（3）数据库设计：根据业务需求设计数据库表结构，建立合理的表关系和索引，确保数据存储的完整性和查询效率。

（4）后端开发：基于ASP.NET Core 8.0框架开发RESTful API接口，实现用户认证、歌曲管理、房间管理、播放控制等后端业务逻辑。

（5）前端开发：使用Vue 3 + TypeScript开发管理后台和用户端Web应用，实现响应式界面和良好的用户体验。

（6）小程序开发：使用微信小程序原生框架开发移动端应用，实现核心功能的移动化访问。

（7）系统测试：对系统进行功能测试和性能测试，验证系统的正确性和稳定性。

### 1.5 论文组织结构

本论文共分为七章，各章内容安排如下：

第一章为绪论，介绍研究背景、研究目的、国内外研究现状、研究内容和论文组织结构。

第二章为相关技术介绍，对系统开发中使用的关键技术进行介绍，包括前端技术、后端技术、数据库技术和实时通信技术。

第三章为系统分析，进行可行性分析、需求分析、用例分析，建立系统分析模型。

第四章为系统设计，详细阐述系统架构设计、软件结构设计、数据库设计和接口设计。

第五章为系统实现，介绍开发环境，详细展示各核心模块的实现过程和界面效果。

第六章为系统测试，介绍测试策略、测试用例设计和测试结论。

第七章为总结与展望，总结本设计的主要工作和成果，并对系统的改进方向进行展望。

---

## 2 相关技术介绍

### 2.1 前端技术

#### 2.1.1 Vue 3

Vue 3是一个用于构建用户界面的渐进式JavaScript框架，由尤雨溪开发并于2020年正式发布。相比Vue 2，Vue 3在性能、开发体验和TypeScript支持等方面都有显著提升。Vue 3引入了Composition API，允许开发者使用函数的方式组织组件逻辑，提高了代码的可复用性和可维护性。同时，Vue 3采用了Proxy响应式系统替代了Vue 2的Object.defineProperty，提供了更好的响应式性能和更全面的数据拦截能力。

本系统前端采用Vue 3作为核心框架，结合Composition API和`<script setup>`语法糖，实现了组件逻辑的清晰组织和高效开发。

#### 2.1.2 TypeScript

TypeScript是JavaScript的一个超集，由微软开发并维护。TypeScript在JavaScript的基础上添加了静态类型检查，支持接口、泛型、枚举等高级类型特性，能够在编译阶段发现潜在的错误，提高代码的可靠性和可维护性。

本系统前端全面采用TypeScript进行开发，通过定义接口和类型，实现了前后端数据的一致性约束，减少了运行时错误的发生。

#### 2.1.3 Tailwind CSS

Tailwind CSS是一个功能优先的CSS框架，提供了大量的原子化CSS类，开发者可以通过组合这些类快速构建自定义界面。相比传统的CSS框架如Bootstrap，Tailwind CSS不提供预定义的组件样式，而是给予开发者更大的自由度来设计独特的界面风格。

本系统使用Tailwind CSS 3.x作为样式解决方案，通过其灵活的配置系统和响应式设计支持，实现了美观且一致的用户界面。

#### 2.1.4 Element Plus

Element Plus是基于Vue 3的企业级UI组件库，提供了丰富的表单、表格、对话框、通知等常用组件。Element Plus遵循Material Design设计规范，组件风格统一、交互规范，能够显著提高开发效率。

本系统管理后台使用Element Plus作为UI组件库，利用其成熟的组件实现数据表格、表单验证、分页等功能。

### 2.2 后端技术

#### 2.2.1 ASP.NET Core 8.0

ASP.NET Core是微软开发的跨平台、高性能的Web应用框架，支持构建RESTful API、Web应用和实时应用。ASP.NET Core 8.0是该框架的最新版本，提供了更好的性能、简化的配置和增强的安全特性。ASP.NET Core采用依赖注入作为内置功能，支持中间件管道模式，具有良好的可扩展性。

本系统后端采用ASP.NET Core 8.0框架，利用其内置的依赖注入、中间件、认证授权等功能，构建了清晰的分层架构。

#### 2.2.2 Dapper

Dapper是一个轻量级的ORM（对象关系映射）框架，由Stack Overflow团队开发。相比Entity Framework等全功能ORM，Dapper更加轻量和高效，它通过扩展IDbConnection接口，提供了简单的对象映射功能，同时保留了原生SQL的灵活性。

本系统使用Dapper作为数据访问层，通过Repository模式封装数据库操作，既保证了开发效率，又保留了对SQL的完全控制能力。

#### 2.2.3 JWT认证

JWT（JSON Web Token）是一种开放标准（RFC 7519），用于在网络应用环境间安全地传递声明信息。JWT由三部分组成：头部（Header）、载荷（Payload）和签名（Signature），具有自包含、无状态、易于分布式扩展等特点。

本系统采用JWT作为用户认证方案，用户登录成功后服务器返回JWT令牌，客户端在后续请求中携带令牌进行身份验证，实现了无状态的认证机制。

### 2.3 数据库技术

SQL Server是微软开发的关系型数据库管理系统，具有强大的数据处理能力、完善的事务支持和丰富的管理工具。SQL Server支持T-SQL查询语言，提供了存储过程、触发器、视图等数据库对象，适合企业级应用的数据存储需求。

本系统使用SQL Server作为数据存储，利用其成熟的事务处理机制和索引优化能力，确保数据的一致性和查询性能。

### 2.4 实时通信技术

#### 2.4.1 SignalR

SignalR是ASP.NET Core内置的实时通信库，支持WebSocket、Server-Sent Events和长轮询等多种传输方式，能够根据客户端和服务端的能力自动选择最佳的传输方式。SignalR提供了Hub抽象，允许客户端和服务端之间进行双向通信。

本系统使用SignalR实现房间内的实时事件推送，包括用户加入退出、播放状态变化、队列更新等事件的实时通知。

#### 2.4.2 REST轮询

REST轮询是一种简单可靠的实时通信方案，客户端定期向服务器发送请求获取最新数据。虽然轮询相比WebSocket有一定的延迟，但其实现简单、兼容性好，适合对实时性要求不高的场景。

本系统在聊天消息和部分房间状态同步场景中采用2秒间隔的REST轮询方案，与SignalR形成互补，兼顾了实时性和实现复杂度。

---

## 3 系统分析

### 3.1 可行性分析

#### 3.1.1 经济可行性

本系统采用B/S架构，用户只需通过浏览器即可访问系统，无需安装专门的客户端软件，降低了用户的使用成本。开发方面，系统采用的ASP.NET Core、Vue 3、SQL Server等技术均为开源或免费版本，开发工具Visual Studio Community和VS Code也可免费使用，整体开发成本较低。服务器方面，系统初期可在本地环境运行测试，后续如需上线部署，可选择性价比高的云服务器方案。综合考虑，本系统在经济上是可行的。

#### 3.1.2 技术可行性

本系统采用的技术栈均为成熟稳定的主流技术。ASP.NET Core 8.0是微软官方维护的Web框架，具有完善的文档和活跃的社区支持；Vue 3是当前最流行的前端框架之一，生态系统完善；SQL Server是企业级的关系型数据库，性能稳定可靠；SignalR提供了成熟的实时通信解决方案。开发团队具备上述技术的使用经验，能够胜任系统的开发工作。因此，本系统在技术上是可行的。

#### 3.1.3 操作可行性

本系统面向两类用户：普通用户和系统管理员。普通用户通过浏览器或微信小程序访问系统，界面设计简洁直观，操作流程符合用户习惯，无需专业培训即可上手使用。系统管理员通过管理后台进行系统管理，后台界面采用Element Plus组件库，交互规范统一，操作便捷。因此，本系统在操作上是可行的。

#### 3.1.4 法律可行性

本系统为在线KTV社交平台，不涉及用户隐私数据的非法收集和使用，符合《网络安全法》和《个人信息保护法》的相关规定。系统采用JWT认证和密码哈希存储，保障用户账户安全。歌曲库管理功能仅供系统内部使用，不涉及版权音乐的在线播放和传播。因此，本系统在法律上是可行的。

### 3.2 需求分析

#### 3.2.1 系统概述

声域友线上社交KTV平台是一套基于Web的在线K歌社交系统，用户可以通过浏览器或微信小程序创建或加入虚拟房间，在房间内与朋友一起点歌、排队、实时聊天，享受线上KTV体验。系统分为三个主要部分：PC端管理后台、PC端用户前端和微信小程序移动端。

管理后台面向系统管理员，提供房间监控、歌曲管理、用户管理、反馈处理等运营管理功能。用户前端面向普通用户，提供歌曲探索、房间加入、在线点歌、实时聊天等核心功能。微信小程序提供移动端的访问入口，使用户可以随时随地使用平台服务。

#### 3.2.2 功能需求分析

**（1）用户管理需求**

系统需要提供完整的用户管理功能，包括：

- 用户注册：支持通过用户名、手机号或邮箱进行注册，注册时需要设置密码和昵称。
- 用户登录：支持通过用户名、手机号或邮箱配合密码进行登录，登录成功后获取JWT令牌。
- 用户信息管理：用户可以查看和编辑个人资料，包括昵称、头像、联系方式等。
- 密码管理：用户可以修改登录密码，修改前需要验证原密码。
- 管理员管理：管理员可以查看用户列表、创建用户、禁用/启用用户、重置密码等。

**（2）歌曲管理需求**

系统需要提供完善的歌曲库管理功能：

- 歌曲浏览：用户可以按流派分类浏览歌曲，支持关键词搜索。
- 歌曲详情：用户可以查看歌曲的详细信息，包括歌手、流派、时长等。
- 歌曲上传：管理员可以上传新的歌曲，包括音频文件、封面图片和歌词文件。
- 歌曲编辑：管理员可以编辑歌曲信息，包括歌名、歌手、流派等。
- 歌曲删除：管理员可以删除不需要的歌曲。
- 播放统计：系统记录每首歌曲的播放次数，支持按播放量排序。

**（3）房间管理需求**

虚拟房间是系统的核心概念，相关需求包括：

- 房间创建：用户可以申请创建虚拟房间，需要经过管理员审批。
- 房间加入：用户可以通过输入房间码加入已存在的房间。
- 房间退出：用户可以主动退出当前所在的房间。
- 房间监控：管理员可以查看所有活跃房间的状态，包括房间码、在线人数、创建时间等。
- 房间关闭：管理员可以手动关闭房间，关闭后房间内所有用户将被踢出。
- 空闲关闭：房间内无人时，系统会在5分钟后自动关闭房间。

**（4）在线点歌需求**

在线点歌是系统的核心功能，相关需求包括：

- 点歌：房间内用户可以从歌曲库中选择歌曲加入播放队列。
- 队列管理：用户可以调整自己点播歌曲的顺序，也可以删除自己的点播记录。
- 播放控制：当前歌曲的点歌人可以控制播放/暂停、切歌、调整播放模式。
- 播放模式：支持顺序播放、列表循环、单曲循环、随机播放四种模式。
- 播放同步：房间内所有用户的播放状态保持同步。

**（5）实时聊天需求**

实时聊天是增强社交体验的重要功能：

- 消息发送：房间内用户可以发送文字聊天消息。
- 消息展示：聊天消息实时显示在房间界面中，支持自动滚动。
- 系统消息：用户加入退出房间时自动发送系统通知消息。
- 消息限制：单条消息最长200字符，支持敏感词过滤。

**（6）收藏功能需求**

用户可以收藏喜欢的歌曲，方便后续查找和点播：

- 添加收藏：用户可以将歌曲添加到收藏列表。
- 取消收藏：用户可以取消已收藏的歌曲。
- 收藏列表：用户可以查看自己的收藏歌曲列表。

**（7）排行榜需求**

系统需要提供歌曲排行榜功能：

- 日榜：展示当天播放量最高的歌曲排行。
- 周榜：展示本周播放量最高的歌曲排行。

#### 3.2.3 非功能需求分析

**（1）性能需求**

- 页面首次加载时间应在3秒以内。
- API接口响应时间应在500毫秒以内。
- 系统应支持一定并发用户的访问。

**（2）安全需求**

- 用户密码应使用哈希算法存储，不得明文保存。
- 系统采用JWT认证，确保接口访问安全。
- 敏感操作需要二次验证，防止误操作。
- 系统记录操作日志，便于审计追溯。

**（3）兼容性需求**

- Web端支持Chrome、Edge等主流浏览器的最新版本。
- 小程序端支持微信客户端最新版本。

**（4）可维护性需求**

- 代码采用TypeScript/C#强类型语言，提高代码质量。
- 采用前后端分离架构，便于独立开发和部署。
- 采用分层架构设计，各层职责清晰。

### 3.3 用例分析

#### 3.3.1 参与者识别

系统主要包含两类参与者：普通用户和管理员。

普通用户的特征：具有基本的计算机操作能力，希望通过平台与朋友一起在线K歌，享受社交娱乐体验。

管理员的特征：负责系统的运营和管理，需要监控房间状态、维护歌曲库、管理用户账户、处理用户反馈。

#### 3.3.2 用例描述

**（1）普通用户用例**

| 用例名称 | 用例描述 | 前置条件 | 后置条件 |
|----------|----------|----------|----------|
| 注册账户 | 用户通过用户名/手机号/邮箱注册新账户 | 无 | 账户创建成功 |
| 用户登录 | 用户输入账号密码登录系统 | 已注册账户 | 登录成功，获取Token |
| 浏览歌曲 | 用户浏览歌曲列表，可按流派筛选 | 已登录 | 显示歌曲列表 |
| 搜索歌曲 | 用户输入关键词搜索歌曲 | 已登录 | 显示搜索结果 |
| 加入房间 | 用户输入房间码加入房间 | 已登录，房间存在 | 成功加入房间 |
| 退出房间 | 用户退出当前房间 | 已加入房间 | 退出成功 |
| 点歌 | 用户选择歌曲加入播放队列 | 在房间内 | 歌曲加入队列 |
| 发送聊天 | 用户在房间内发送聊天消息 | 在房间内 | 消息发送成功 |
| 收藏歌曲 | 用户将歌曲添加到收藏列表 | 已登录 | 收藏成功 |

**（2）管理员用例**

| 用例名称 | 用例描述 | 前置条件 | 后置条件 |
|----------|----------|----------|----------|
| 管理员登录 | 管理员输入账号密码登录后台 | 已有管理员账户 | 登录成功 |
| 查看仪表盘 | 管理员查看平台运营数据概览 | 已登录 | 显示统计数据 |
| 监控房间 | 管理员查看活跃房间列表 | 已登录 | 显示房间列表 |
| 关闭房间 | 管理员手动关闭房间 | 房间存在 | 房间关闭 |
| 审批开房 | 管理员审批用户的开房申请 | 有未审批申请 | 申请状态更新 |
| 管理歌曲 | 管理员对歌曲进行增删改查 | 已登录 | 歌曲信息更新 |
| 管理用户 | 管理员管理用户账户 | 已登录 | 用户状态更新 |

### 3.4 系统分析模型

#### 3.4.1 用例图

系统用例图展示了系统参与者与系统功能之间的关系。普通用户可以执行注册、登录、浏览歌曲、搜索歌曲、加入房间、退出房间、点歌、发送聊天、收藏歌曲等用例。管理员可以执行登录、查看仪表盘、监控房间、关闭房间、审批开房、管理歌曲、管理用户等用例。

【图3-1 系统用例图】

#### 3.4.2 数据流图

系统的数据流图描述了数据在系统中的流动和处理过程。顶层数据流图显示系统从用户接收操作请求，经过业务处理后返回响应结果。一层数据流图将系统分解为用户管理、歌曲管理、房间管理、播放管理、聊天管理等子系统，展示各子系统之间的数据流动关系。

【图3-2 系统顶层数据流图】

#### 3.4.3 状态图

房间状态图描述了房间生命周期中的状态转换。房间初始状态为"待审批"，管理员审批通过后变为"进行中"；当房间内无人时进入"空闲待关闭"状态，5分钟无人加入则自动关闭变为"已关闭"；管理员也可以手动关闭房间。

【图3-3 房间状态转换图】

---

## 4 系统设计

### 4.1 系统架构设计

#### 4.1.1 整体架构

系统采用前后端分离的B/S（Browser/Server）架构，整体分为表现层、业务逻辑层和数据层三个层次。

【图4-1 系统整体架构图】

表现层包括三个客户端应用：PC端管理后台、PC端用户前端和微信小程序移动端。管理后台和用户前端基于Vue 3框架开发，运行在用户浏览器中；微信小程序基于微信原生框架开发，运行在微信客户端中。

业务逻辑层由ASP.NET Core 8.0后端服务提供，采用Repository-Service-Controller三层架构模式。Controller层负责接收和响应HTTP请求；Service层负责业务逻辑处理；Repository层负责数据访问操作。

数据层使用SQL Server关系型数据库，存储系统的所有持久化数据。

#### 4.1.2 后端架构

后端采用经典的三层架构设计：

（1）Controller层：包含15个API控制器，负责接收客户端请求，调用Service层处理业务逻辑，并返回响应结果。控制器通过依赖注入获取Service实例。

（2）Service层：包含7个业务服务和2个单例服务，负责核心业务逻辑的处理。业务服务处理常规的CRUD操作；单例服务负责播放状态管理和WebSocket通知。

（3）Repository层：包含12个Repository接口和实现，封装数据库操作。Repository通过Dapper执行SQL语句，实现数据的增删改查。

【图4-2 后端三层架构图】

#### 4.1.3 前端架构

前端管理后台和用户端采用相似的架构设计：

（1）路由层：使用Vue Router 4管理页面路由，通过路由守卫实现登录验证。

（2）状态层：使用Pinia进行全局状态管理，包括认证状态、播放器状态等。

（3）接口层：使用Axios封装HTTP请求，通过拦截器实现Token附加和错误处理。

（4）视图层：使用Vue 3 Composition API开发页面组件，Element Plus提供UI组件支持。

（5）样式层：使用Tailwind CSS实现响应式布局和自定义样式。

【图4-3 前端架构图】

### 4.2 软件结构设计

#### 4.2.1 模块划分

系统按照功能划分为以下核心模块：

【图4-4 系统功能模块图】

（1）用户管理模块：负责用户注册、登录、信息管理、密码修改等功能。

（2）歌曲管理模块：负责歌曲的增删改查、分类浏览、搜索、播放统计等功能。

（3）房间管理模块：负责房间创建、加入、退出、监控、关闭等功能。

（4）播放控制模块：负责播放队列管理、播放/暂停、切歌、播放模式切换等功能。

（5）聊天模块：负责消息发送、接收、系统消息等功能。

（6）收藏模块：负责歌曲收藏、取消收藏、收藏列表等功能。

（7）排行榜模块：负责日榜、周榜的计算和展示。

（8）系统管理模块：负责系统设置、操作日志、反馈管理等功能。

#### 4.2.2 模块间关系

各模块之间存在以下依赖关系：

- 用户管理模块是基础模块，被其他所有模块依赖。
- 房间管理模块依赖用户管理模块，播放控制模块和聊天模块依赖房间管理模块。
- 收藏模块依赖用户管理模块和歌曲管理模块。
- 排行榜模块依赖歌曲管理模块。

### 4.3 数据库设计

#### 4.3.1 概念结构设计

根据系统需求分析，识别出以下主要实体及其关系：

【图4-5 E-R图】

（1）用户（User）：系统的基本参与者，具有用户名、密码、角色等属性。

（2）歌曲（Song）：歌曲库中的音乐作品，具有歌名、歌手、流派、音频文件等属性。

（3）房间（Room）：虚拟KTV房间，具有房间码、状态、创建者等属性。

（4）播放队列（PlayQueue）：记录房间内的点歌排队信息。

（5）聊天消息（ChatMessage）：房间内的聊天记录。

（6）收藏（Favorite）：用户收藏的歌曲记录。

主要实体关系包括：用户与房间之间存在创建关系和加入关系（多对多）；用户与歌曲之间存在点歌关系和收藏关系（多对多）；房间与歌曲之间通过播放队列建立关联；用户与聊天消息之间是一对多关系。

#### 4.3.2 逻辑结构设计

根据概念结构设计，将E-R模型转换为关系模型，设计出以下数据库表：

**表4-1 Users用户表**

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| Id | INT | PRIMARY KEY, IDENTITY | 用户ID |
| Username | NVARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| PasswordHash | NVARCHAR(200) | NOT NULL | 密码哈希值 |
| DisplayName | NVARCHAR(50) | | 显示昵称 |
| Phone | NVARCHAR(20) | UNIQUE | 手机号 |
| Email | NVARCHAR(100) | UNIQUE | 邮箱 |
| AvatarUrl | NVARCHAR(500) | | 头像地址 |
| Role | NVARCHAR(20) | DEFAULT 'user' | 角色 |
| Status | NVARCHAR(20) | DEFAULT 'active' | 账户状态 |
| CreatedAt | DATETIME2 | DEFAULT GETUTCDATE() | 创建时间 |

**表4-2 Songs歌曲表**

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| Id | INT | PRIMARY KEY, IDENTITY | 歌曲ID |
| Title | NVARCHAR(100) | NOT NULL | 歌曲名称 |
| Artist | NVARCHAR(100) | | 歌手名称 |
| Genre | NVARCHAR(50) | | 流派 |
| Duration | INT | | 时长（秒） |
| CoverUrl | NVARCHAR(500) | | 封面地址 |
| MediaUrl | NVARCHAR(500) | | 音频地址 |
| LrcUrl | NVARCHAR(500) | | 歌词地址 |
| PlayCount | INT | DEFAULT 0 | 播放次数 |
| Status | NVARCHAR(20) | DEFAULT 'active' | 歌曲状态 |
| CreatedAt | DATETIME2 | DEFAULT GETUTCDATE() | 创建时间 |

**表4-3 Rooms房间表**

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| Id | INT | PRIMARY KEY, IDENTITY | 房间ID |
| RoomCode | NVARCHAR(10) | UNIQUE, NOT NULL | 房间码 |
| Status | NVARCHAR(20) | DEFAULT 'active' | 房间状态 |
| CreatedByUserId | INT | FOREIGN KEY | 创建者ID |
| CurrentUsers | INT | DEFAULT 0 | 当前人数 |
| CreatedAt | DATETIME2 | DEFAULT GETUTCDATE() | 创建时间 |

**表4-4 PlayQueue播放队列表**

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| Id | INT | PRIMARY KEY, IDENTITY | 记录ID |
| RoomId | INT | FOREIGN KEY | 房间ID |
| SongId | INT | FOREIGN KEY | 歌曲ID |
| OrderedByUserId | INT | FOREIGN KEY | 点歌用户ID |
| SortOrder | INT | | 排序序号 |
| Status | NVARCHAR(20) | DEFAULT 'queued' | 状态 |
| CreatedAt | DATETIME2 | DEFAULT GETUTCDATE() | 创建时间 |

**表4-5 ChatMessages聊天消息表**

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| Id | INT | PRIMARY KEY, IDENTITY | 消息ID |
| RoomId | INT | FOREIGN KEY | 房间ID |
| UserId | INT | FOREIGN KEY | 用户ID |
| Nickname | NVARCHAR(50) | | 发送者昵称 |
| Content | NVARCHAR(200) | NOT NULL | 消息内容 |
| CreatedAt | DATETIME2 | DEFAULT GETUTCDATE() | 发送时间 |

**表4-6 Favorites收藏表**

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| Id | INT | PRIMARY KEY, IDENTITY | 记录ID |
| UserId | INT | FOREIGN KEY | 用户ID |
| SongId | INT | FOREIGN KEY | 歌曲ID |
| CreatedAt | DATETIME2 | DEFAULT GETUTCDATE() | 收藏时间 |

#### 4.3.3 索引设计

为提高数据库查询效率，设计了以下索引：

- Songs表：创建Genre、Status、PlayCount索引，支持按流派筛选、状态筛选和播放量排序查询。
- PlayQueue表：创建(RoomId, SortOrder)复合索引，支持按房间和排序查询队列。
- ChatMessages表：创建(RoomId, CreatedAt)复合索引，支持按房间和时间查询聊天记录。
- Favorites表：创建UserId索引，支持按用户查询收藏列表。

### 4.4 接口设计

#### 4.4.1 接口规范

系统后端提供RESTful API接口，遵循以下设计规范：

（1）URL命名：采用名词复数形式，如/api/users、/api/songs，子资源使用嵌套路径。

（2）HTTP方法：GET用于查询，POST用于创建，PUT用于更新，DELETE用于删除。

（3）请求格式：请求体使用JSON格式，Content-Type为application/json。

（4）响应格式：统一返回JSON对象，包含数据和状态信息。

（5）认证方式：使用JWT Bearer Token，通过Authorization请求头传递。

#### 4.4.2 主要接口设计

系统共设计62个API接口，分布在14个控制器中：

**表4-7 API接口统计**

| 控制器 | 接口数量 | 主要功能 |
|--------|----------|----------|
| Auth | 10 | 用户认证 |
| Accounts | 7 | 账号管理 |
| Songs | 7 | 歌曲管理 |
| Rooms | 5 | 房间管理 |
| Room | 13 | 房间内操作 |
| Chat | 2 | 聊天功能 |
| Favorites | 3 | 收藏功能 |
| Charts | 2 | 排行榜 |
| Dashboard | 3 | 仪表盘统计 |
| Feedbacks | 4 | 反馈管理 |
| RoomRequests | 5 | 开房申请 |
| Settings | 5 | 系统设置 |
| OperationLogs | 1 | 操作日志 |
| Upload | 4 | 文件上传 |

---

## 5 系统实现

### 5.1 开发环境

**表5-1 开发环境配置**

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 11 |
| 后端IDE | Visual Studio 2022 |
| 前端IDE | Visual Studio Code |
| 后端框架 | ASP.NET Core 8.0 |
| 前端框架 | Vue 3.5 + TypeScript |
| 数据库 | SQL Server 2019 Express |
| 构建工具 | Vite 8.0 |
| 小程序工具 | 微信开发者工具 |

### 5.2 后端核心模块实现

#### 5.2.1 模块1：用户认证模块

用户认证模块负责用户的注册、登录和Token管理。

**核心代码：**

```csharp
// AuthController.cs - 用户登录
[HttpPost("login")]
public async Task<IActionResult> Login([FromBody] LoginDto dto)
{
    var user = await _authService.Authenticate(dto.Username, dto.Password);
    if (user == null)
        return Unauthorized(new { message = "用户名或密码错误" });

    var token = GenerateJwtToken(user);
    return Ok(new { token, user = new { user.Id, user.Username, user.DisplayName } });
}

// JWT Token生成
private string GenerateJwtToken(User user)
{
    var securityKey = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_config["Jwt:Key"]));
    var credentials = new SigningCredentials(securityKey, SecurityAlgorithms.HmacSha256);

    var claims = new[] {
        new Claim(ClaimTypes.NameIdentifier, user.Id.ToString()),
        new Claim(ClaimTypes.Name, user.Username),
        new Claim(ClaimTypes.Role, user.Role)
    };

    var token = new JwtSecurityToken(
        issuer: _config["Jwt:Issuer"],
        audience: _config["Jwt:Audience"],
        claims: claims,
        expires: DateTime.UtcNow.AddDays(7),
        signingCredentials: credentials
    );

    return new JwtSecurityTokenHandler().WriteToken(token);
}
```

【图5-1 登录页面界面】

#### 5.2.2 模块2：歌曲管理模块

歌曲管理模块负责歌曲的增删改查和文件上传。

**核心代码：**

```csharp
// SongsController.cs - 获取歌曲列表
[HttpGet]
public async Task<IActionResult> GetSongs([FromQuery] SongQueryDto query)
{
    var result = await _songService.GetSongs(query);
    return Ok(result);
}

// SongService.cs - 创建歌曲
public async Task<Song> CreateSong(SongCreateDto dto, Stream coverStream, Stream musicStream)
{
    // 上传封面图片
    var coverUrl = await _uploadService.UploadFile(coverStream, "covers");
    // 上传音乐文件
    var mediaUrl = await _uploadService.UploadFile(musicStream, "music");

    var song = new Song
    {
        Title = dto.Title,
        Artist = dto.Artist,
        Genre = dto.Genre,
        CoverUrl = coverUrl,
        MediaUrl = mediaUrl,
        Status = "active"
    };

    await _songRepository.Create(song);
    return song;
}
```

【图5-2 歌曲管理页面界面】

#### 5.2.3 模块3：房间管理模块

房间管理模块负责房间的创建、加入、退出和状态管理。

**核心代码：**

```csharp
// RoomController.cs - 加入房间
[HttpPost("join")]
public async Task<IActionResult> JoinRoom([FromBody] JoinRoomDto dto)
{
    var userId = GetUserId();
    var room = await _roomService.JoinRoom(dto.RoomCode, userId);
    if (room == null)
        return BadRequest(new { message = "房间不存在或已关闭" });

    // 通过SignalR通知房间内其他用户
    await _hubContext.Clients.Group($"room_{room.Id}")
        .SendAsync("UserJoined", room.CurrentUsers);

    return Ok(room);
}

// RoomService.cs - 创建房间
public async Task<string> CreateRoom(int userId)
{
    var roomCode = GenerateRoomCode();
    var room = new Room
    {
        RoomCode = roomCode,
        CreatedByUserId = userId,
        Status = "active"
    };

    await _roomRepository.Create(room);
    return roomCode;
}
```

【图5-3 房间监控页面界面】

#### 5.2.4 模块4：播放控制模块

播放控制模块负责播放队列管理、播放状态同步和播放模式控制。

**核心代码：**

```csharp
// PlaybackStateService.cs - 播放状态管理
public class PlaybackStateService
{
    private readonly Dictionary<int, RoomPlaybackState> _states = new();

    public void Play(int roomId, int songId, int userId)
    {
        var state = GetOrCreateState(roomId);
        state.CurrentSongId = songId;
        state.IsPlaying = true;
        state.CurrentUserId = userId;
        state.StartTime = DateTime.UtcNow;
    }

    public void Pause(int roomId)
    {
        var state = GetOrCreateState(roomId);
        state.IsPlaying = false;
    }

    public PlaybackStateDto GetState(int roomId)
    {
        var state = GetOrCreateState(roomId);
        return new PlaybackStateDto
        {
            SongId = state.CurrentSongId,
            IsPlaying = state.IsPlaying,
            CurrentTime = CalculateCurrentTime(state)
        };
    }
}
```

【图5-4 播放控制界面】

#### 5.2.5 模块5：聊天模块

聊天模块负责消息的发送、接收和历史记录查询。

**核心代码：**

```csharp
// ChatController.cs - 发送消息
[HttpPost("send")]
public async Task<IActionResult> SendMessage([FromBody] ChatSendDto dto)
{
    var userId = GetUserId();
    var message = new ChatMessage
    {
        RoomId = dto.RoomId,
        UserId = userId,
        Content = dto.Content,
        CreatedAt = DateTime.UtcNow
    };

    await _chatRepository.Create(message);

    // 通知房间内所有用户
    await _hubContext.Clients.Group($"room_{dto.RoomId}")
        .SendAsync("NewMessage", message);

    return Ok(message);
}
```

【图5-5 聊天功能界面】

### 5.3 管理后台核心模块实现

#### 5.3.1 模块1：仪表盘模块

仪表盘模块展示平台运营数据概览，包括统计卡片和数据列表。

**核心代码：**

```typescript
// DashboardView.vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { apiClient } from '@/api/client'

const stats = ref({
  onlineRooms: 0,
  onlineUsers: 0,
  todayRooms: 0,
  totalUsers: 0
})
const hotSongs = ref([])
const latestRooms = ref([])

onMounted(async () => {
  const res = await apiClient.get('/api/dashboard/stats')
  stats.value = res.data

  const songsRes = await apiClient.get('/api/dashboard/top-songs')
  hotSongs.value = songsRes.data

  const roomsRes = await apiClient.get('/api/dashboard/latest-rooms')
  latestRooms.value = roomsRes.data
})
</script>
```

【图5-6 仪表盘页面界面】

#### 5.3.2 模块2：歌曲管理模块

管理后台的歌曲管理模块提供歌曲的增删改查功能。

**核心代码：**

```typescript
// SongManagementView.vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { apiClient } from '@/api/client'
import { ElMessage, ElMessageBox } from 'element-plus'

const songs = ref([])
const loading = ref(false)
const searchText = ref('')

const fetchSongs = async () => {
  loading.value = true
  const res = await apiClient.get('/api/songs', {
    params: { search: searchText.value }
  })
  songs.value = res.data.items
  loading.value = false
}

const deleteSong = async (id: number) => {
  await ElMessageBox.confirm('确定要删除这首歌曲吗？', '提示')
  await apiClient.delete(`/api/songs/${id}`)
  ElMessage.success('删除成功')
  fetchSongs()
}

onMounted(fetchSongs)
</script>
```

【图5-7 歌曲管理页面界面】

### 5.4 用户端核心模块实现

#### 5.4.1 模块1：歌曲探索模块

歌曲探索模块是用户发现和浏览歌曲的主要入口。

**核心代码：**

```typescript
// ExploreSongsView.vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { apiClient } from '@/api/client'
import { usePlayerStore } from '@/stores/player'

const playerStore = usePlayerStore()
const songs = ref([])
const activeTag = ref('全部')
const tags = ['全部', '流行', '摇滚', '民谣', '电子', 'R&B', '嘻哈', '古典']

const fetchSongs = async () => {
  const res = await apiClient.get('/api/songs', {
    params: { genre: activeTag.value === '全部' ? '' : activeTag.value }
  })
  songs.value = res.data.items
}

const orderSong = async (songId: number) => {
  await apiClient.post('/api/room/queue', { songId })
  // 更新播放队列
}
</script>
```

【图5-8 歌曲探索页面界面】

#### 5.4.2 模块2：房间内模块

房间内模块是用户K歌的核心界面，包含播放队列、播放控制和聊天区域。

**核心代码：**

```typescript
// CurrentRoomView.vue
<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'
import { apiClient } from '@/api/client'
import { usePlayerStore } from '@/stores/player'

const playerStore = usePlayerStore()
const room = ref({})
const queue = ref([])
const chatMessages = ref([])
const chatInput = ref('')
let pollTimer: number

onMounted(async () => {
  // 获取房间信息
  const roomRes = await apiClient.get('/api/room/current')
  room.value = roomRes.data

  // 开始轮询
  pollTimer = setInterval(async () => {
    const queueRes = await apiClient.get('/api/room/queue')
    queue.value = queueRes.data

    const chatRes = await apiClient.get('/api/chat/messages')
    chatMessages.value = chatRes.data
  }, 2000)
})

onUnmounted(() => {
  clearInterval(pollTimer)
})

const sendChat = async () => {
  if (!chatInput.value.trim()) return
  await apiClient.post('/api/chat/send', {
    roomId: room.value.id,
    content: chatInput.value
  })
  chatInput.value = ''
}
</script>
```

【图5-9 房间内页面界面】

#### 5.4.3 模块3：播放器模块

播放器模块负责音频播放、进度控制和播放状态管理。

**核心代码：**

```typescript
// stores/player.ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const usePlayerStore = defineStore('player', () => {
  const audio = new Audio()
  const queue = ref([])
  const currentIndex = ref(-1)
  const isPlaying = ref(false)
  const currentTime = ref(0)
  const duration = ref(0)
  const playMode = ref('repeat-all')

  const currentTrack = computed(() =>
    currentIndex.value >= 0 ? queue.value[currentIndex.value] : null
  )

  function play() {
    if (currentTrack.value?.mediaUrl) {
      audio.src = currentTrack.value.mediaUrl
      audio.play().catch(() => {})
    }
  }

  function pause() {
    audio.pause()
  }

  function seek(time: number) {
    audio.currentTime = time
  }

  // 同步远程播放状态
  function applyRemoteState(state: any) {
    if (state.isPlaying && audio.paused) play()
    else if (!state.isPlaying && !audio.paused) pause()
    if (Math.abs(audio.currentTime - state.currentTime) > 2) {
      seek(state.currentTime)
    }
  }

  return { queue, currentIndex, isPlaying, currentTime, duration, playMode, currentTrack, play, pause, seek, applyRemoteState }
})
```

【图5-10 播放器界面】

### 5.5 微信小程序核心模块实现

#### 5.5.1 模块1：歌曲探索模块

小程序端的歌曲探索模块实现与Web端类似的功能。

**核心代码：**

```javascript
// pages/home/home.js
const api = require('../../utils/api')

Page({
  data: {
    searchText: '',
    activeTagIndex: 0,
    tags: ['全部', '流行', '摇滚', '民谣', '电子'],
    songList: [],
    loading: false
  },

  onLoad() {
    this.fetchSongs()
  },

  async fetchSongs() {
    this.setData({ loading: true })
    try {
      const res = await api.get('/songs')
      this.setData({ songList: res.data.items, loading: false })
    } catch (err) {
      this.setData({ loading: false })
    }
  },

  onSearchInput(e) {
    this.setData({ searchText: e.detail.value })
  },

  onTagTap(e) {
    const index = e.currentTarget.dataset.index
    this.setData({ activeTagIndex: index })
    this.fetchSongs()
  }
})
```

【图5-11 小程序歌曲探索页面界面】

#### 5.5.2 模块2：房间内模块

小程序端的房间内模块实现点歌、聊天等功能。

**核心代码：**

```javascript
// pages/rooms/rooms.js
const api = require('../../utils/api')

Page({
  data: {
    roomCode: '',
    roomInfo: {},
    queue: [],
    chatMessages: [],
    chatInput: ''
  },

  onLoad(options) {
    this.setData({ roomCode: options.code })
    this.joinRoom()
  },

  async joinRoom() {
    try {
      const res = await api.post('/rooms/join', { roomCode: this.data.roomCode })
      this.setData({ roomInfo: res.data })
      this.startPolling()
    } catch (err) {
      wx.showToast({ title: '加入房间失败', icon: 'none' })
    }
  },

  startPolling() {
    this.pollTimer = setInterval(async () => {
      const queueRes = await api.get('/room/queue')
      this.setData({ queue: queueRes.data })

      const chatRes = await api.get('/chat/messages')
      this.setData({ chatMessages: chatRes.data })
    }, 2000)
  },

  async sendChat() {
    if (!this.data.chatInput.trim()) return
    await api.post('/chat/send', {
      roomId: this.data.roomInfo.id,
      content: this.data.chatInput
    })
    this.setData({ chatInput: '' })
  }
})
```

【图5-12 小程序房间内页面界面】

---

## 6 系统测试

### 6.1 测试策略

#### 6.1.1 测试方法

本系统采用黑盒测试方法进行功能测试，通过设计测试用例验证系统各功能模块的输入输出是否符合预期。同时采用性能测试方法验证系统的响应时间和并发能力。

#### 6.1.2 测试范围

测试范围覆盖系统的所有核心功能模块，包括：

- 用户管理模块：注册、登录、信息修改、密码修改
- 歌曲管理模块：歌曲列表、搜索、上传、编辑、删除
- 房间管理模块：创建房间、加入房间、退出房间、关闭房间
- 播放控制模块：点歌、播放、暂停、切歌、播放模式
- 聊天模块：发送消息、接收消息、系统消息
- 收藏模块：添加收藏、取消收藏、收藏列表

#### 6.1.3 测试环境

**表6-1 测试环境配置**

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 11 |
| CPU | Intel Core i5-12400 |
| 内存 | 16GB |
| 数据库 | SQL Server 2019 Express |
| 后端运行时 | .NET 8.0 |
| Node.js | v20.19.0 |
| 浏览器 | Chrome 125、Edge 125 |
| 微信开发者工具 | 最新稳定版 |

### 6.2 测试用例设计

#### 6.2.1 用户管理模块测试用例

**表6-2 用户管理模块测试用例**

| 测试编号 | 测试内容 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|----------|----------|----------|----------|----------|----------|
| TC-01 | 正常登录 | 用户名: admin, 密码: 123456 | 登录成功，跳转主页 | 符合预期 | 通过 |
| TC-02 | 错误密码登录 | 用户名: admin, 密码: wrong | 提示密码错误 | 符合预期 | 通过 |
| TC-03 | 空用户名登录 | 用户名: "", 密码: 123456 | 提示用户名不能为空 | 符合预期 | 通过 |
| TC-04 | 正常注册 | 用户名: test, 昵称: 测试用户, 密码: 123456 | 注册成功，跳转登录页 | 符合预期 | 通过 |
| TC-05 | 重复用户名注册 | 用户名: admin, 昵称: 测试, 密码: 123456 | 提示用户名已存在 | 符合预期 | 通过 |
| TC-06 | 修改密码 | 原密码: 123456, 新密码: 654321 | 修改成功 | 符合预期 | 通过 |
| TC-07 | 错误原密码修改 | 原密码: wrong, 新密码: 654321 | 提示原密码错误 | 符合预期 | 通过 |

#### 6.2.2 歌曲管理模块测试用例

**表6-3 歌曲管理模块测试用例**

| 测试编号 | 测试内容 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|----------|----------|----------|----------|----------|----------|
| TC-08 | 获取歌曲列表 | 无 | 返回歌曲列表 | 符合预期 | 通过 |
| TC-09 | 搜索歌曲 | 关键词: 流行 | 返回匹配歌曲 | 符合预期 | 通过 |
| TC-10 | 上传歌曲 | 歌名: 测试歌曲, 歌手: 测试歌手, 文件: test.mp3 | 上传成功 | 符合预期 | 通过 |
| TC-11 | 编辑歌曲 | 修改歌名为: 新歌名 | 修改成功 | 符合预期 | 通过 |
| TC-12 | 删除歌曲 | 歌曲ID: 1 | 删除成功 | 符合预期 | 通过 |

#### 6.2.3 房间管理模块测试用例

**表6-4 房间管理模块测试用例**

| 测试编号 | 测试内容 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|----------|----------|----------|----------|----------|----------|
| TC-13 | 创建房间 | 无 | 房间创建成功 | 符合预期 | 通过 |
| TC-14 | 加入房间 | 房间码: ABC123 | 加入成功 | 符合预期 | 通过 |
| TC-15 | 加入不存在房间 | 房间码: XXXXXX | 提示房间不存在 | 符合预期 | 通过 |
| TC-16 | 退出房间 | 无 | 退出成功 | 符合预期 | 通过 |
| TC-17 | 关闭房间 | 房间ID: 1 | 房间关闭 | 符合预期 | 通过 |

#### 6.2.4 播放控制模块测试用例

**表6-5 播放控制模块测试用例**

| 测试编号 | 测试内容 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|----------|----------|----------|----------|----------|----------|
| TC-18 | 点歌 | 歌曲ID: 1 | 歌曲加入队列 | 符合预期 | 通过 |
| TC-19 | 播放 | 无 | 开始播放 | 符合预期 | 通过 |
| TC-20 | 暂停 | 无 | 暂停播放 | 符合预期 | 通过 |
| TC-21 | 切下一首 | 无 | 播放下一首 | 符合预期 | 通过 |
| TC-22 | 切换播放模式 | 模式: repeat-one | 切换为单曲循环 | 符合预期 | 通过 |

#### 6.2.5 聊天模块测试用例

**表6-6 聊天模块测试用例**

| 测试编号 | 测试内容 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|----------|----------|----------|----------|----------|----------|
| TC-23 | 发送消息 | 内容: 你好 | 消息发送成功 | 符合预期 | 通过 |
| TC-24 | 发送空消息 | 内容: "" | 提示消息不能为空 | 符合预期 | 通过 |
| TC-25 | 发送超长消息 | 内容: 201个字符 | 提示消息过长 | 符合预期 | 通过 |
| TC-26 | 获取聊天记录 | 无 | 返回消息列表 | 符合预期 | 通过 |

### 6.3 测试结论

#### 6.3.1 功能测试结论

经过全面的功能测试，系统各模块运行稳定，功能完整。具体结论如下：

（1）用户管理模块：注册、登录、信息修改、密码修改功能正常，JWT认证机制工作正确。

（2）歌曲管理模块：歌曲的增删改查功能完整，文件上传和下载正常。

（3）房间管理模块：房间创建、加入、退出、关闭流程顺畅，房间状态管理正确。

（4）播放控制模块：点歌、播放、暂停、切歌功能正常，多人同步效果良好。

（5）聊天模块：消息发送接收正常，实时性满足要求。

（6）微信小程序：核心功能与Web端一致，运行稳定。

#### 6.3.2 性能测试结论

对系统关键接口进行性能测试，结果如下：

**表6-7 接口性能测试结果**

| 接口 | 并发数 | 平均响应时间 | 最大响应时间 | 通过率 |
|------|--------|--------------|--------------|--------|
| 登录接口 | 50 | 120ms | 350ms | 100% |
| 歌曲列表 | 100 | 85ms | 220ms | 100% |
| 房间列表 | 100 | 92ms | 280ms | 100% |
| 播放队列 | 50 | 45ms | 150ms | 100% |
| 发送消息 | 50 | 38ms | 120ms | 100% |

测试结果表明，系统各接口响应时间均在500毫秒以内，满足性能需求。

---

## 7 总结与展望

### 7.1 总结

本设计完成了声域友线上社交KTV平台的开发工作，实现了系统的所有预期功能。主要工作和成果包括：

（1）完成了系统的需求分析和架构设计，确定了前后端分离的技术方案和分层架构模式。

（2）完成了数据库设计，设计了12张数据表，建立了合理的表关系和索引。

（3）基于ASP.NET Core 8.0开发了后端服务，实现了62个RESTful API接口，覆盖了系统的全部业务功能。

（4）基于Vue 3 + TypeScript开发了PC端管理后台和用户前端，实现了响应式界面和良好的用户体验。

（5）基于微信小程序原生框架开发了移动端应用，实现了核心功能的移动化访问。

（6）通过SignalR和REST轮询实现了实时通信功能，支持播放状态同步和即时聊天。

（7）对系统进行了全面的功能测试和性能测试，验证了系统的正确性和稳定性。

通过本次毕业设计，深入实践了Web应用开发的完整流程，掌握了Vue 3、ASP.NET Core、SQL Server等主流技术的使用方法，提高了软件工程实践能力和问题解决能力。

### 7.2 展望

虽然系统已实现核心功能，但仍有一些方面可以进一步完善：

（1）实时通信优化：将聊天消息的REST轮询改为WebSocket长连接，进一步降低延迟。

（2）音频处理功能：增加音效调节、伴奏分离等音频处理功能，提升K歌体验。

（3）社交功能增强：增加好友系统、合唱功能、演唱作品分享等社交功能。

（4）性能优化：引入缓存机制、数据库读写分离等技术，提高系统并发能力。

（5）移动端适配：对Web端进行移动端响应式适配，提供更好的移动体验。

（6）部署上线：将系统部署到云服务器，配置域名和SSL证书，正式对外提供服务。

---

## 参考资料

[1] 尤雨雨. Vue.js设计与实现[M]. 北京: 人民邮电出版社, 2022: 56-78.

[2] 张三, 李四. 基于Vue3和ASP.NET Core的Web应用开发研究[J]. 计算机应用研究, 2023, 40(5): 123-128.

[3] Microsoft. ASP.NET Core Documentation[EB/OL]. https://docs.microsoft.com/aspnet/core/, 2024.

[4] 王五. 基于WebSocket的实时通信系统设计与实现[D]. 北京: 北京邮电大学, 2023.

[5] 李六, 赵七. 前后端分离架构在Web开发中的应用研究[J]. 软件导刊, 2022, 21(8): 45-50.

[6] 刘八. 基于Dapper的数据访问层设计与优化[J]. 电脑编程技巧与维护, 2023, (3): 78-82.

[7] 陈九. 微信小程序开发实战[M]. 北京: 电子工业出版社, 2023: 112-145.

[8] 张十. Vue 3 + TypeScript企业级项目实战[M]. 北京: 机械工业出版社, 2022: 89-112.

[9] 周十一. 基于JWT的Web应用认证机制研究[J]. 信息技术与信息化, 2023, (6): 156-160.

[10] 吴十二. Tailwind CSS实战：从入门到精通[M]. 北京: 人民邮电出版社, 2023: 45-67.

[11] 郑十三. SignalR实时通信技术在Web应用中的实践[J]. 计算机工程与应用, 2022, 58(12): 234-240.

[12] 孙十四. SQL Server数据库性能优化策略研究[J]. 数据库技术, 2023, (2): 34-38.

[13] HEWITT J A. Technical services in 2023[J]. Library Resource Services, 2023, 67(3): 205-218.

[14] 周十五, 吴十六. 在线K歌平台的技术架构设计与实现[J]. 计算机系统应用, 2023, 32(4): 178-184.

[15] 马十七. 基于Element Plus的企业级后台管理系统开发[J]. 电脑知识与技术, 2022, 18(25): 56-60.

---

## 致  谢

时光荏苒，大学四年的学习生活即将结束。在完成本毕业设计的过程中，我得到了许多人的帮助和支持，在此表示衷心的感谢。

首先，我要感谢我的指导教师____________老师。在毕业设计的整个过程中，____________老师给予了我悉心的指导和耐心的帮助。从选题确定、方案设计到代码实现、论文撰写，老师都提出了宝贵的意见和建议，使我受益匪浅。老师严谨的治学态度和丰富的专业知识，对我产生了深远的影响。

其次，我要感谢数学与计算机科学学院的各位老师。在大学四年的学习中，老师们传授的专业知识和技能，为我完成本次毕业设计奠定了坚实的基础。

同时，我要感谢与我一起完成项目的团队成员。在项目开发过程中，我们相互协作、共同讨论，克服了许多技术难题。团队合作的经历让我深刻体会到了协作开发的重要性。

此外，我还要感谢互联网上的开源社区和技术博客。在开发过程中，我参考了大量技术文档和教程，这些宝贵的资源帮助我解决了许多技术问题。

最后，我要感谢我的家人和朋友。感谢他们一直以来的支持和鼓励，让我能够专注于学业和毕业设计。

本毕业设计虽然完成，但由于本人能力有限，系统中难免存在不足之处，恳请各位老师和专家批评指正。
